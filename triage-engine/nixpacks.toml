[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python scripts/startup.py && uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"